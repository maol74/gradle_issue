/*
tasks.register('clean') {
    group = 'build'
    dependsOn gradle.includedBuild('my-conventions').task(':clean')
}

tasks.register('build') {
    group = 'build'
    dependsOn gradle.includedBuild('my-conventions').task(':build')
}
*/

